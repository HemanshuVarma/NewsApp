<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="PaddingStyle">
        <item name="android:paddingLeft">@dimen/four_dips</item>
        <item name="android:paddingRight">@dimen/four_dips</item>
        <item name="android:paddingStart">@dimen/four_dips</item>
        <item name="android:paddingEnd">@dimen/four_dips</item>
    </style>
</resources>
